//2021-5-22 19:56
//³ÂÈñ
/*#include<iostream>
#include<iomanip>
using namespace std;
int main()
{
	double a, b, c, d;
	cin >> a >> b >> c >> d;
	int num = 0;
	for (double i = -100; i < 100; i+=0.001)
	{
		double l = i,j=l+0.001;
			if ((a * l * l * l + b * l * l + c * l + d) * (a * j * j * j + b * j * j + c * j + d) < 0)
			{
				double zhi = l;
				cout << fixed << setprecision(2) << zhi << " ";
				num++;
			}
			if (num == 3)
				break;
	}
	system("pause");
	return 0;
}*/